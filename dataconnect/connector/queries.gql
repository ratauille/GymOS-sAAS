# GraphQL Queries for GymOS Data Connect

query ListGyms @auth(level: PUBLIC) {
  gyms {
    id
    name
    slug
    ownerId
    createdAt
  }
}

query ListLeadsCRM @auth(level: PUBLIC) {
  leads {
    id
    fullName
    email
    phone
    goal
    planInterest
    status
    createdAt
  }
}
